  // pass in you tube api key, query string and max returned result and a call back 
var searchYouTube = (key, query, maxResults, callback) => {
// use jquerry get to start the request 
    // pass in api url 
    // pass in properties 
        // part 
        // key 
        // query
        // max result 
        // type
        // embeddable 
    //on done, iterate through the result 
        // u call back if passed in on all items 
    // if fail , log response json 
        // log each error 
//esport you tube search function to the window 
};

window.searchYouTube = searchYouTube;
